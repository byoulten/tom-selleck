kind delete cluster
kind create cluster
kubectl cluster-info --context kind-kind
rm ./deployements/*
kompose convert -o deployments && kubectl apply -f ./deployments
kubectl get deployment,svc,pods,pvc

#when client pod is running, execute kubectl port-forward service/client 8080:3000 and go to localhost:8080 to view site

